# F21 — evidência de eval para o F23 (D10): parcial por test file + merge.
# A evidência é artefato de run; o CI a regenera a cada execução.
test/eval/evidence/

# F29 — memória persistente (D1/QA-1a): o DB local de memória é dado de
# runtime do repo (mesmo padrão dos demais sinks .runecraft/*).
.runecraft/memory/
