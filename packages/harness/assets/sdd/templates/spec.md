# {{feature}} — Specification

**Scope:** {{scope}} (preenchido pelo autor — ver `.specs/project/STATE.md` e `harness sdd chains`)
**Prereq (roadmap):** {{prereq}}
**Grupo:** (grupo/feature do roadmap)

## Problem Statement

{{objective}}

## Goals

- [ ] (goal 1 — critério verificável)
- [ ] (goal 2)
- [ ] (goal 3)

## Out of Scope

| Feature | Reason |
| --- | --- |
| (fora de escopo) | (motivo) |

## Gray area (resolver antes do Execute)

Opções + recomendação (QA-1..QA-n); o Execute NÃO começa sem as respostas:

- **QA-1 — (tema)**: (a) **recomendado — (opção)** · (b) (alternativa)

**Já decidido (não é gray area):** (decisões travadas: zero deps novas; offline/$0; escopo packages/harness; determinismo; etc.)

## User Stories

### P1: (título) ⭐ MVP — (requirement IDs)

**User Story**: (como usuário, quero ... para que ...)

**Why P1**: (justificativa)

**Acceptance Criteria**:

1. WHEN ... THEN ...
2. WHEN ... THEN ...

**Independent Test**: (como o critério é provado isoladamente)

### P2: (título) — (requirement IDs)

**User Story**: ...

**Why P2**: ...

**Acceptance Criteria**:

1. WHEN ... THEN ...

**Independent Test**: ...

## Edge Cases

- WHEN ... THEN ...
- WHEN ... THEN ...

## Requirement Traceability

| Requirement ID | Story | Phase | Status |
| --- | --- | --- | --- |
| (ID) | (story) | (Design/Execute/Review) | {{status}} |

**Coverage:** N total, N mapeados, N unmapped (mapeamento em design.md e tasks.md)

## Success Criteria

- [ ] (critério 1)
- [ ] (critério 2)
- [ ] Fronteiras explícitas: (donos por feature)
- [ ] ≤N open questions para o usuário (QA-1..QA-n)
